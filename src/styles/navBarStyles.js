import { css } from '@mui/system';

const classes = {
  innerBox: css({
    flex: 1,
    display: "flex",
  }),
  navItem: css({
    marginLeft: "50px",
    // color: "#389AF4",
    color: "inherit",
    fontWeight: "500",
    textDecoration: "none",
    cursor: "pointer",
  }),
}

export default classes;